# PS5 connection (optional — auto-discovers if blank)
PS5_IP=

# Server ports (optional — defaults work out of the box)
WS_PORT=4401

# AI Race Engineer (optional — get a key at https://aistudio.google.com/apikey)
GEMINI_API_KEY=
